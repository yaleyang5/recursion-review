// this is what you would do if you were one to do things the easy way:
// var parseJSON = JSON.parse;

// but you're not, so you'll write it from scratch:
// I: json string
// O: the variable that string is supposed to represent (either arr or obj)
// C: idk
// E: Null? Undefined?

// helper function for strings and ints and stuff
var helper(input) {}


var parseJSON = function(json) {
  // return variable
  // if string starts with "[" its an arr, otherwise
};
